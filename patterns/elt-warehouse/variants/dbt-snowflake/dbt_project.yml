name: chakra_lakehouse
version: "1.0.0"
config-version: 2

profile: chakra

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  chakra_lakehouse:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
